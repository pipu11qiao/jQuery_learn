exec()方法用于检索字符串中的正则表达式的匹配

返回一个数组，其中存放匹配的结果。如果未找到匹配，则返回值为null

exec（）方法的功能非常强大，它是一个通用的方法，而且使用起来也比test（）方法以及支持正则表达式的String对象的防范更为组咋
如果exec（）找到了匹配的文本，则返回一个结果数组，否则放回null。次数组的第0个元素是与正则表达式向匹配的文本，第一个元素是
与RegExpObject的第一个子表达式向匹配的文本（如果有的话），第二个元素是与RegExpObject的第二个子表达式相匹配的文本，以此
类推。除了数组元素和和length属性之外，exec（）方法还返回两个属性。index属性声明的是匹配文本的第一个字符的位置。input属性
则存放的是被检索的字符串string。我们可以看得出，在调用费全局的regexp对象的exec（）方法时，放回的数组与调用方法String。match（）
返回的数组是相通的。他将把regxpobject的lastindex属性设置为匹配文本的最后一个字符的想一个位置。
